using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TutResourses : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Player.ResourceCount += 100;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
